$('.calendar').fullCalendar('removeEvents', [ <%= @event.id %> ]);
$('.calendar').fullCalendar(
  'renderEvent',
  $.parseJSON(" <%= j render('event.json', event: @event, format: :json).html_safe %>"),
  true
);
$('.modal').modal('hide');
