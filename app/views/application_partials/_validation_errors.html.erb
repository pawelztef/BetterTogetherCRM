<% if object.errors.any? %>
  <div class="validation_errors">
    <div> <%= pluralize(object.errors.count, "error") %> prohibited from being saved:  </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li> <%= msg %> </li>
      <% end %> 
    </ul>
  </div>
<% end %> 
