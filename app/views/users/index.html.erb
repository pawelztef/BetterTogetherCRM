<h1>users#index</h1>
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Fullname</th>
      <th>Email</th>
      <th>Created at</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td> <%= user.fullname %> </td>
        <td> <%= user.email %> </td>
        <td> <%= user.created_at %> </td>
        <td> <%= link_to "Edit", edit_user_path(user) %> 
        <%= link_to "Delete", user_path(user), method: 'delete' %> </td>
      </tr>
    <% end %> 
  </tbody>
</table>

